
# Database settings.
database:

  #  JDBC driver
  driverClass: org.h2.Driver
  # the JDBC URL
  url: jdbc:h2:./target/food.db
  
  properties:
    charSet: UTF-8
    hibernate.dialect: org.hibernate.dialect.MySQL5InnoDBDialect  
    hibernate.hbm2ddl.auto: update